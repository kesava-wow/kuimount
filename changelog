* Added a keybind. It should be somewhere near the bottom of the default UI's keybind frame, under "Kui Mount".
* work around an issue where the API returns usable for cloud serpents while standing in water even if you don't have the relevant skill